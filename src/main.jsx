import React,{useEffect,useMemo,useState}from'react';
import{createRoot}from'react-dom/client';
import{BarChart3,ScanLine,PackageCheck,Factory,Clock3,LogOut,UserRound,CalendarDays,ShieldCheck,Target,TriangleAlert,UsersRound,Trophy,UserPlus,LayoutDashboard,CheckCircle2,XCircle}from'lucide-react';
import{supabase,supabaseConfigured}from'./supabase';
import'./styles.css';

const setores=[{id:1,nome:'1. Corte e destaque'},{id:2,nome:'2. Cola e EVA'},{id:3,nome:'3. Colagem do couro'},{id:4,nome:'4. Limpeza'},{id:5,nome:'5. Finalização e alça'},{id:6,nome:'6. Embalagem'}];
const modelos=['Redondo 60x60','Gota 54x60','Redondo 40x40','Orgânica 90x50'];
const META=200;
const nomeSetor=id=>setores.find(s=>s.id===id)?.nome.replace(/^\d\. /,'')||'Sem setor';
function inicioPeriodo(p){let d=new Date();d.setHours(0,0,0,0);if(p==='7d')d.setDate(d.getDate()-6);if(p==='30d')d.setDate(d.getDate()-29);return d}
function diasPeriodo(p){return p==='hoje'?1:p==='7d'?7:30}

function Login({onSession}){
 const[email,setEmail]=useState(''),[senha,setSenha]=useState(''),[msg,setMsg]=useState(''),[load,setLoad]=useState(false);
 async function entrar(e){e.preventDefault();setLoad(true);const{data,error}=await supabase.auth.signInWithPassword({email,password:senha});setLoad(false);if(error)return setMsg('Não foi possível entrar. Confira e-mail e senha.');onSession(data.session)}
 async function cadastrar(){if(!email||!senha)return setMsg('Informe e-mail e senha.');setLoad(true);const{error}=await supabase.auth.signUp({email,password:senha,options:{emailRedirectTo:'https://gustavomultinformatica-crypto.github.io/sistema-producao-espelhos/'}});setLoad(false);setMsg(error?error.message:'Acesso criado. Confirme o e-mail se solicitado e depois entre normalmente.')}
 return <main className="loginPage"><section className="loginCard"><div className="loginIcon"><Factory/></div><span className="eyebrow">CONTROLE DE FÁBRICA</span><h1>Sistema de Produção de Espelhos</h1><p>Entre para registrar e acompanhar a produção.</p><form onSubmit={entrar}><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Senha<input type="password" value={senha} onChange={e=>setSenha(e.target.value)}/></label><button disabled={load}>{load?'ENTRANDO...':'ENTRAR'}</button></form><button className="secondary" onClick={cadastrar}>CRIAR NOVO USUÁRIO</button>{msg&&<p className="message">{msg}</p>}</section></main>
}

function PerfilForm({session,onSave}){
 const[nome,setNome]=useState(''),[setor,setSetor]=useState(1),[load,setLoad]=useState(false),[msg,setMsg]=useState('');
 useEffect(()=>{(async()=>{const{data}=await supabase.from('funcionarios_pendentes').select('nome,setor_id').eq('email',session.user.email).maybeSingle();if(data){setNome(data.nome);setSetor(data.setor_id||1)}})()},[session]);
 async function salvar(e){e.preventDefault();if(!nome.trim())return setMsg('Informe o nome do funcionário.');setLoad(true);const{data,error}=await supabase.from('perfis').upsert({usuario_id:session.user.id,nome:nome.trim(),email:session.user.email,setor_id:setor,ativo:true,atualizado_em:new Date().toISOString()}).select().single();setLoad(false);if(error)return setMsg(error.message);onSave(data)}
 return <main className="loginPage"><section className="profileSetup panel"><div className="panelTitle"><UsersRound/><div><h2>Complete seu cadastro</h2><p>Seu nome e setor identificarão todas as bipagens.</p></div></div><form onSubmit={salvar}><label>Nome completo<input value={nome} onChange={e=>setNome(e.target.value)} placeholder="Nome do funcionário"/></label><label>Setor principal<select value={setor} onChange={e=>setSetor(Number(e.target.value))}>{setores.map(s=><option key={s.id} value={s.id}>{s.nome}</option>)}</select></label><button disabled={load}>{load?'SALVANDO...':'SALVAR PERFIL'}</button></form>{msg&&<p className="message">{msg}</p>}</section></main>
}

function Funcionarios({perfis,pendentes,onReload}){
 const[nome,setNome]=useState(''),[email,setEmail]=useState(''),[setor,setSetor]=useState(1),[msg,setMsg]=useState(''),[load,setLoad]=useState(false);
 async function adicionar(e){e.preventDefault();if(!nome.trim()||!email.trim())return setMsg('Informe nome e e-mail.');setLoad(true);const{error}=await supabase.from('funcionarios_pendentes').upsert({nome:nome.trim(),email:email.trim().toLowerCase(),setor_id:setor,ativo:true},{onConflict:'email'});setLoad(false);if(error)return setMsg(error.message);setNome('');setEmail('');setMsg('Funcionário pré-cadastrado. Ele já pode criar o acesso usando esse mesmo e-mail.');await onReload()}
 async function alterarPerfil(usuarioId,campo,valor){const{error}=await supabase.from('perfis').update({[campo]:valor,atualizado_em:new Date().toISOString()}).eq('usuario_id',usuarioId);if(error)return setMsg(error.message);await onReload()}
 async function alterarPendente(id,campo,valor){const{error}=await supabase.from('funcionarios_pendentes').update({[campo]:valor}).eq('id',id);if(error)return setMsg(error.message);await onReload()}
 return <section className="adminSection"><div className="adminGrid"><div className="panel"><div className="panelTitle"><UserPlus/><div><h2>Cadastrar funcionário</h2><p>Pré-cadastre antes de criar o login.</p></div></div><form onSubmit={adicionar}><label>Nome completo<input value={nome} onChange={e=>setNome(e.target.value)} placeholder="Ex.: João da Silva"/></label><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="funcionario@empresa.com"/></label><label>Setor principal<select value={setor} onChange={e=>setSetor(Number(e.target.value))}>{setores.map(s=><option key={s.id} value={s.id}>{s.nome}</option>)}</select></label><button disabled={load}>{load?'SALVANDO...':'CADASTRAR FUNCIONÁRIO'}</button></form>{msg&&<p className="message">{msg}</p>}<div className="infoBox">Depois do pré-cadastro, o funcionário abre a tela de login e usa <b>CRIAR NOVO USUÁRIO</b> com o mesmo e-mail.</div></div><div className="panel"><div className="panelTitle"><UsersRound/><div><h2>Resumo da equipe</h2><p>{perfis.length} acessos vinculados • {pendentes.length} pré-cadastros</p></div></div><div className="teamStats"><div><strong>{perfis.filter(p=>p.ativo).length}</strong><span>Ativos</span></div><div><strong>{perfis.filter(p=>!p.ativo).length}</strong><span>Inativos</span></div><div><strong>{pendentes.filter(p=>!perfis.some(x=>x.email?.toLowerCase()===p.email?.toLowerCase())).length}</strong><span>Aguardando acesso</span></div></div></div></div>
 <section className="panel recent"><div className="panelTitle"><UsersRound/><div><h2>Funcionários com acesso</h2><p>Edite setor ou ative/desative o operador.</p></div></div>{perfis.length===0?<div className="empty">Nenhum funcionário com acesso vinculado.</div>:<div className="employeeList">{perfis.map(p=><div className="employeeRow" key={p.usuario_id}><div className="employeeName"><b>{p.nome}</b><small>{p.email||'E-mail não registrado'} • {p.papel==='admin'?'Administrador':'Operador'}</small></div><select value={p.setor_id||1} onChange={e=>alterarPerfil(p.usuario_id,'setor_id',Number(e.target.value))}>{setores.map(s=><option key={s.id} value={s.id}>{s.nome}</option>)}</select><button className={p.ativo?'statusBtn activeStatus':'statusBtn inactiveStatus'} onClick={()=>alterarPerfil(p.usuario_id,'ativo',!p.ativo)}>{p.ativo?<><CheckCircle2 size={15}/> Ativo</>:<><XCircle size={15}/> Inativo</>}</button></div>)}</div>}</section>
 <section className="panel recent"><div className="panelTitle"><UserPlus/><div><h2>Pré-cadastros</h2><p>Funcionários aguardando ou já vinculados a um login.</p></div></div>{pendentes.length===0?<div className="empty">Nenhum pré-cadastro.</div>:<div className="employeeList">{pendentes.map(p=>{const vinculado=perfis.some(x=>x.email?.toLowerCase()===p.email?.toLowerCase());return <div className="employeeRow" key={p.id}><div className="employeeName"><b>{p.nome}</b><small>{p.email} • {vinculado?'Acesso vinculado':'Aguardando criar acesso'}</small></div><select value={p.setor_id||1} onChange={e=>alterarPendente(p.id,'setor_id',Number(e.target.value))}>{setores.map(s=><option key={s.id} value={s.id}>{s.nome}</option>)}</select><span className={`pill ${vinculado?'linked':'waiting'}`}>{vinculado?'Vinculado':'Pendente'}</span></div>})}</div>}</section></section>
}

function App(){
 const[session,setSession]=useState(null),[ready,setReady]=useState(!supabaseConfigured),[codigo,setCodigo]=useState(''),[modelo,setModelo]=useState(modelos[0]),[setorId,setSetorId]=useState(1),[regs,setRegs]=useState([]),[msg,setMsg]=useState(''),[tipo,setTipo]=useState(''),[periodo,setPeriodo]=useState('hoje'),[salvando,setSalvando]=useState(false),[perfil,setPerfil]=useState(null),[perfis,setPerfis]=useState([]),[pendentes,setPendentes]=useState([]),[tela,setTela]=useState('painel');
 useEffect(()=>{if(!supabaseConfigured)return;supabase.auth.getSession().then(({data})=>{setSession(data.session);setReady(true);if(data.session)carregarTudo(data.session)});const{data:l}=supabase.auth.onAuthStateChange((_e,s)=>{setSession(s);if(s)carregarTudo(s)});return()=>l.subscription.unsubscribe()},[]);
 async function carregarTudo(sess=session){await carregar();const{data:ps}=await supabase.from('perfis').select('usuario_id,nome,email,setor_id,ativo,papel').order('nome');setPerfis(ps||[]);const meu=(ps||[]).find(p=>p.usuario_id===sess?.user?.id)||null;setPerfil(meu);if(meu?.setor_id)setSetorId(meu.setor_id);if(meu?.papel==='admin'){const{data:fp}=await supabase.from('funcionarios_pendentes').select('id,nome,email,setor_id,ativo').order('nome');setPendentes(fp||[])}}
 async function carregar(){const{data,error}=await supabase.from('bipagens').select('id,criado_em,setor_id,usuario_id,produtos(id,codigo_barras,modelo)').gte('criado_em',inicioPeriodo('30d').toISOString()).order('criado_em',{ascending:false});if(error)return setMsg('Não foi possível carregar a produção.');setRegs((data||[]).map(r=>({id:r.id,produtoId:r.produtos?.id,codigo:r.produtos?.codigo_barras||'-',modelo:r.produtos?.modelo||'-',setorId:r.setor_id,usuarioId:r.usuario_id,criadoEm:r.criado_em,hora:new Date(r.criado_em).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})})))}
 const filtrados=useMemo(()=>regs.filter(r=>new Date(r.criadoEm)>=inicioPeriodo(periodo)),[regs,periodo]);
 const totais=useMemo(()=>setores.map(s=>({...s,total:filtrados.filter(r=>r.setorId===s.id).length})),[filtrados]);
 const pecas=useMemo(()=>new Set(filtrados.map(r=>r.codigo)).size,[filtrados]);
 const embaladas=totais[5]?.total||0,metaPeriodo=META*diasPeriodo(periodo),progresso=Math.min(100,Math.round((embaladas/metaPeriodo)*100)),gargalo=useMemo(()=>totais.reduce((a,b)=>b.total<a.total?b:a,totais[0]),[totais]),minhaProd=filtrados.filter(r=>r.usuarioId===session?.user?.id).length;
 const ranking=useMemo(()=>perfis.filter(p=>p.ativo).map(p=>({...p,total:filtrados.filter(r=>r.usuarioId===p.usuario_id).length})).sort((a,b)=>b.total-a.total),[perfis,filtrados]);
 async function validar(produtoId,destino){const{data,error}=await supabase.from('bipagens').select('setor_id').eq('produto_id',produtoId).order('setor_id');if(error)return{ok:false,texto:'Falha ao validar histórico.'};const e=new Set((data||[]).map(x=>x.setor_id));if(e.has(destino))return{ok:false,texto:`Peça já registrada no setor ${destino}.`};if(destino>1&&!e.has(destino-1))return{ok:false,texto:`Etapa fora de ordem. Primeiro registre em ${setores[destino-2].nome}.`};return{ok:true}}
 async function bipar(e){e.preventDefault();const v=codigo.trim();if(!v||salvando)return;if(perfil&&!perfil.ativo)return setMsg('Seu usuário está desativado. Procure o administrador.');if(perfil?.setor_id&&setorId!==perfil.setor_id)return setMsg(`Seu usuário está vinculado ao setor ${perfil.setor_id}.`);setSalvando(true);setMsg('');let{data:p,error:b}=await supabase.from('produtos').select('id,modelo').eq('codigo_barras',v).maybeSingle();if(b){setSalvando(false);return setMsg('Erro ao consultar peça.')}if(!p){if(setorId!==1){setSalvando(false);return setMsg('Peça nova deve iniciar no setor 1.')}const{data,error}=await supabase.from('produtos').insert({codigo_barras:v,modelo}).select('id,modelo').single();if(error){setSalvando(false);return setMsg('Erro ao cadastrar peça.')}p=data}const val=await validar(p.id,setorId);if(!val.ok){setSalvando(false);return setMsg(val.texto)}const{error}=await supabase.from('bipagens').insert({produto_id:p.id,setor_id:setorId,usuario_id:session.user.id});setSalvando(false);if(error)return setMsg('Erro ao registrar bipagem: '+error.message);setCodigo('');setTipo('sucesso');setMsg(`Peça ${v} registrada com sucesso.`);await carregar()}
 async function sair(){await supabase.auth.signOut();setSession(null)}
 if(!ready)return <main className="loginPage"><section className="loginCard"><p>Carregando...</p></section></main>;
 if(!session)return <Login onSession={setSession}/>;
 if(!perfil)return <PerfilForm session={session} onSave={p=>{setPerfil(p);setPerfis(x=>[...x.filter(y=>y.usuario_id!==p.usuario_id),p]);setSetorId(p.setor_id)}}/>;
 return <main><header><div><span className="eyebrow">CONTROLE DE FÁBRICA</span><h1>Sistema de Produção de Espelhos</h1></div><div className="headerActions"><div className="user"><UserRound size={17}/>{perfil.nome}</div><button className="logout" onClick={sair}><LogOut size={16}/>Sair</button></div></header>
 <nav className="appNav"><button className={tela==='painel'?'active':''} onClick={()=>setTela('painel')}><LayoutDashboard size={17}/> Painel</button>{perfil.papel==='admin'&&<button className={tela==='funcionarios'?'active':''} onClick={()=>setTela('funcionarios')}><UsersRound size={17}/> Funcionários</button>}</nav>
 {tela==='funcionarios'&&perfil.papel==='admin'?<Funcionarios perfis={perfis} pendentes={pendentes} onReload={()=>carregarTudo(session)}/>:<><section className="periodBar"><div className="periodTitle"><CalendarDays size={18}/><strong>Relatório:</strong></div>{[['hoje','Hoje'],['7d','7 dias'],['30d','30 dias']].map(([v,t])=><button key={v} className={periodo===v?'active':''} onClick={()=>setPeriodo(v)}>{t}</button>)}</section><section className="cards managementCards"><article><Target/><div><small>Meta do período</small><strong>{embaladas}/{metaPeriodo}</strong><p>{progresso}% concluído</p></div></article><article><TriangleAlert/><div><small>Gargalo atual</small><strong className="smallStrong">{gargalo?nomeSetor(gargalo.id):'-'}</strong><p>{gargalo?.total||0} passagens</p></div></article><article><UserRound/><div><small>Minha produtividade</small><strong>{minhaProd}</strong><p>bipagens no período</p></div></article></section><div className="goalPanel"><div><b>Meta de produção</b><span>{META} peças embaladas por dia</span></div><div className="progressTrack"><div className="progressFill" style={{width:`${progresso}%`}}/></div><strong>{progresso}%</strong></div><section className="cards"><article><Factory/><div><small>Peças movimentadas</small><strong>{pecas}</strong><p>peças únicas</p></div></article><article><PackageCheck/><div><small>Embaladas</small><strong>{embaladas}</strong><p>concluídas</p></div></article><article><BarChart3/><div><small>Total de bipagens</small><strong>{filtrados.length}</strong><p>passagens registradas</p></div></article></section><section className="grid"><div className="panel scanner"><div className="panelTitle"><ScanLine/><div><h2>Bipagem de produção</h2><p>Operador: {perfil.nome}</p></div></div><form onSubmit={bipar}><label>Setor<select value={setorId} disabled>{setores.map(s=><option key={s.id} value={s.id}>{s.nome}</option>)}</select></label><label>Modelo<select value={modelo} onChange={e=>setModelo(e.target.value)}>{modelos.map(m=><option key={m}>{m}</option>)}</select></label><label>Código de barras<input autoFocus value={codigo} onChange={e=>setCodigo(e.target.value)} placeholder="Bipe o código aqui..."/></label><button disabled={salvando||!perfil.ativo}>{salvando?'REGISTRANDO...':'REGISTRAR PEÇA'}</button></form>{msg&&<p className={`message ${tipo}`}>{msg}</p>}<div className="sequenceNote"><ShieldCheck size={17}/><span>A peça só avança após a etapa anterior.</span></div></div><div className="panel"><div className="panelTitle"><BarChart3/><div><h2>Produção por setor</h2><p>Meta proporcional de {metaPeriodo} por setor</p></div></div><div className="sectorList">{totais.map((s,i)=><div className={`sector ${s.id===gargalo?.id?'bottleneck':''}`} key={s.id}><span className="number">{i+1}</span><span>{nomeSetor(s.id)}</span><strong>{s.total}</strong></div>)}</div></div></section><section className="panel recent"><div className="panelTitle"><Trophy/><div><h2>Ranking de produtividade</h2><p>Bipagens por funcionário no período</p></div></div>{ranking.length===0?<div className="empty">Nenhum funcionário cadastrado.</div>:<div className="rankingList">{ranking.map((r,i)=><div className="rankRow" key={r.usuario_id}><span className="rankPos">{i+1}º</span><div><b>{r.nome}</b><small>{nomeSetor(r.setor_id)}</small></div><strong>{r.total}</strong></div>)}</div>}</section><section className="panel recent"><div className="panelTitle"><Clock3/><div><h2>Últimas bipagens</h2><p>Movimentações recentes</p></div></div>{filtrados.length===0?<div className="empty">Nenhuma peça registrada.</div>:<div className="table">{filtrados.slice(0,30).map(r=><div className="row" key={r.id}><b>{r.codigo}</b><span>{r.modelo}</span><span>{nomeSetor(r.setorId)}</span><time>{new Date(r.criadoEm).toLocaleDateString('pt-BR')} {r.hora}</time></div>)}</div>}</section></>}
 </main>
}

createRoot(document.getElementById('root')).render(<React.StrictMode><App/></React.StrictMode>);